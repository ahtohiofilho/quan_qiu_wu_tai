# Iniciar o servidor Flask
python start.py server

# Iniciar o cliente PyQt6
python start.py client

# Reinicializar a infra AWS (limpa S3, recria tabelas)
python start.py init

# Criar e fazer upload de um novo mundo
python start.py create-world --fator 5 --bioma "Forest"

# Testar conexão com AWS
python start.py test

# Iniciar a interface web
python start.py interface

# Iniciar o servidor Flask diretamente
python server/app.py

# Iniciar o simulador de players
python start.py simulador

equação da nutrição: x = oferta / demanda; y = coeficiente de nutrição
    n = 2
    m = 3
    y = 1 + (n / m) * (1 - (1 / (1 + (m / (n + m)) * (x - 1))))

def valor_oferta(quantidades, base=2.0):
    # equação do minério
    # ordena do maior para o menor
    quantidades_ordenadas = sorted(quantidades, reverse=True)
    # soma aplicando a fórmula
    return sum(q * (base ** (p)) for p, q in enumerate(quantidades_ordenadas))
